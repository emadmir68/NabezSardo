{
  "name": "nabezsardo",
  "version": "2.0.0",
  "private": true,
  "description": "Independent production newsroom for Nabez Sardo",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy",
    "cf:d1:init": "wrangler d1 execute nabzesardo-db --remote --file=./cloudflare/schema.sql"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
